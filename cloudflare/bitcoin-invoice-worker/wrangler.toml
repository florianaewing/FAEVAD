name = "faevad-bitcoin-invoice"
main = "src/index.js"
compatibility_date = "2026-09-01"

# Secret (never put it here): set with
#   wrangler secret put OPENNODE_API_KEY
# Use an OpenNode API key scoped to "Invoices" permission only.
